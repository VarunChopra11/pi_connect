# Pi-Connect Environment Variables
# Copy this file to .env and update with your actual values

# Shared secret for HMAC authentication
# Must match the secret configured on your Raspberry Pi
# Generate with: openssl rand -base64 32
VITE_SHARED_SECRET=CHANGE_ME_IN_PRODUCTION_USE_ENV_VAR
